<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.buses254">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>

        <activity android:name=".Login"
            android:theme="@style/AppTheme"/>

        <activity android:name=".Register"
            android:theme="@style/AppTheme"/>

        <activity
            android:name=".OneWayTrip"
            android:label="Purchase Ticket"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ReturnTrip"
            android:label="Purchase Ticket"
            android:theme="@style/AppTheme" />

        <activity android:name=".MainActivity" />

        <activity
            android:name=".navDrawer"
            android:label="Buses254"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".NewsFeed"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <activity
            android:name=".oneWaySchedule"
            android:label="Bus Schedule"
            android:theme="@style/AppTheme"></activity>

    </application>
</manifest>